<!DOCTYPE html>
<html>

{{> head}}

<body>
    
    {{#user}}

    <div class="backToU">
        <a href="../users">Back to Users</a>
    </div>
    <div class="userInfo">
        <div class="avaUrl">
            <img src="{{avaUrl}}">
        </div>
        <div class="userBio">
            <p class="fullnameP">{{fullname}}</p>
            <p class="extraInfoP">({{login}})</p>
            <p class="registeredAt">{{registeredAt}}</p>
        </div>
    </div>

    {{/user}}

    {{> footer}}

</body>

</html>